name: Sync Upstream

on:
  schedule:
    # 每天 UTC 0:00 (北京时间 8:00) 自动同步
    - cron: '0 0 * * *'
  workflow_dispatch:  # 支持手动触发

permissions:
  contents: write

jobs:
  sync:
    name: Sync with upstream
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        submodules: false
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Configure Git
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"

    - name: Add upstream remote
      run: |
        git remote add upstream https://github.com/ztxz16/fastllm.git || true
        git fetch upstream

    - name: Sync upstream to dev branch
      id: sync_dev
      run: |
        echo "=== Syncing upstream to dev branch ==="

        # 准备分支：dev 跟踪 upstream/master
        git fetch upstream
        git fetch origin dev || true

        BEFORE_COMMIT=""
        if git show-ref --verify --quiet refs/remotes/origin/dev; then
          git checkout dev
          git pull origin dev
          BEFORE_COMMIT=$(git rev-parse HEAD)
        else
          git checkout -b dev upstream/master
          BEFORE_COMMIT=$(git rev-parse HEAD)
        fi

        # 将 dev 分支重置到 upstream/master（保持纯净上游代码）
        git checkout upstream/master -- .
        git reset --hard upstream/master

        # 从重置前的 dev 提交恢复本仓库的 patches 目录，再应用 Windows 补丁
        git checkout "$BEFORE_COMMIT" -- patches

        echo "Applying Windows patches..."
        chmod +x patches/apply-patches.sh
        bash patches/apply-patches.sh

        # 覆盖 workflows 为本仓库默认分支版本
        DEFAULT_BRANCH="master"
        if git symbolic-ref --short refs/remotes/origin/HEAD >/dev/null 2>&1; then
            DEFAULT_BRANCH=$(git symbolic-ref --short refs/remotes/origin/HEAD | sed 's@^origin/@@')
        fi
        git fetch origin "$DEFAULT_BRANCH"
        git checkout "origin/$DEFAULT_BRANCH" -- .github/workflows || true

        # 提交所有变更
        git add -A
        AFTER_COMMIT=$(git rev-parse HEAD)
        if [ "$(git diff --cached --name-only)" != "" ]; then
          git commit -m "chore: sync upstream/master + apply Windows patches" || true
          AFTER_COMMIT=$(git rev-parse HEAD)
          echo "dev_synced=true" >> $GITHUB_OUTPUT
          echo "✅ dev branch updated from upstream/master + patches applied"
        else
          echo "dev_synced=false" >> $GITHUB_OUTPUT
          echo "ℹ️ dev branch already up to date"
        fi

        # dev 被重置为 upstream/master，需强制推送（保留本地旧提交可在 reflog 找回）
        git push --force-with-lease -u origin dev

    - name: Update submodules on master
      id: submodules
      run: |
        echo "=== Checking submodule updates on master ==="

        git checkout master
        git pull origin master

        # checkout 未初始化子模块，需先 init
        git submodule update --init third_party/pybind11 third_party/sentencepiece
        # 逐个尝试合并到远程分支最新版本；sentencepiece 固定为生产可编译版本，
        # 若与上游无共同历史则保留固定版本，避免破坏 master 构建
        for SUB in third_party/pybind11 third_party/sentencepiece; do
          if git submodule update --remote --merge "$SUB" 2>/dev/null; then
            echo "✓ $SUB 已更新到远程最新版本"
          else
            echo "⚠ $SUB 无法安全合并，保持当前固定版本"
          fi
        done

        # 检查是否有变更
        if git diff --quiet; then
          echo "Submodules are up to date"
          echo "updated=false" >> $GITHUB_OUTPUT
        else
          echo "Submodules updated"
          # 仅提交已跟踪的子模块指针变更,避免把 dev 步骤残留的子模块目录误带入 master
          git add -u
          git commit -m "chore: Auto-update submodules" || true
          git push origin master
          echo "updated=true" >> $GITHUB_OUTPUT
        fi

    - name: Create sync summary
      run: |
        echo "=== Sync Summary ==="
        echo "• dev branch: upstream code + Windows patches (for review/testing)"
        echo "• master branch: production code (submodules only)"

    outputs:
      dev_synced: ${{ steps.sync_dev.outputs.dev_synced }}
      submodules_updated: ${{ steps.submodules.outputs.updated }}
